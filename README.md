# LOR-Local-Onion-Router
Local Onion Router is a low-level, Tor-inspired system that shows secure multi-hop file transfer using layered (onion) encryption over TCP. Each relay decrypts only 1 encryption layer and forwards the data, no single node knows both the sender &amp; final receiver. The project provides a clear, visual simulation of onion routing concepts in a local env
